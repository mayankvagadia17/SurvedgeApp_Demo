# EXECUTE — Modularize MappingFragmentLogic.kt

> Read `03-implementation-sessions.md`.
> Find the first session with an unchecked `[ ]` item.
> Present only that session's diff. Wait for approval.
> After executing and updating the checklist, stop and wait.
> User says "next" to advance.

**⚠️ IMPORTANT: Do NOT build or compile after each session. Only build at the end when the entire plan is complete. Build errors will reference missing files that haven't been created yet.**
